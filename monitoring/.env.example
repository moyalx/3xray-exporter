# Grafana admin login (change the password!).
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=changeme

# Optional: public URL if Grafana sits behind a domain / reverse proxy,
# e.g. https://grafana.example.com
GF_ROOT_URL=

# Prometheus data retention.
PROM_RETENTION=30d
